{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "trashWindow"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:path:default"
  ]
}
